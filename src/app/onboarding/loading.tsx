import { ShellLoading } from "@/components/shell-loading";

/** Renders the prefetched Still route shell while onboarding resolves. */
export default function Loading() {
  return <ShellLoading />;
}
