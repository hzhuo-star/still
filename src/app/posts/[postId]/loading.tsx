import { ShellLoading } from "@/components/shell-loading";

/** Renders the prefetched static shell while a Conversation route resolves. */
export default function Loading() {
  return <ShellLoading />;
}
