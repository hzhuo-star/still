import { ShellLoading } from "@/components/shell-loading";

/** Renders the prefetched Still route shell while a relationship list resolves. */
export default function Loading() {
  return <ShellLoading />;
}
