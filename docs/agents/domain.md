# Domain docs

This is a single-context repository.

Before exploring or changing the product domain:

1. Read the root `CONTEXT.md` and use its canonical language in code, tests, issues, and documentation.
2. Read relevant decisions under `docs/adr/` and surface any proposed contradiction explicitly.
3. Treat a missing concept as a domain-modeling question rather than silently inventing a synonym.
